{
  "name": "<%= _.slugify(ProjectName) %>",
  "version": "0.0.1",
  "main": "sandbox.html",
  "dependencies": {<% if (oldIE) { %>
    "jquery": "~1",<% } %><% if (includeModernizr) { %>
    "modernizr-shim": "~0.0.1",<% } %>
    "requirejs": "~2.1.14",<% if (includeAccordion) { %>
    "accordion": "accordion",<% } %>
  }
}
